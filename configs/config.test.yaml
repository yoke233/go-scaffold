app:
  env: "test"

server:
  http:
    addr: "127.0.0.1:18080"
  grpc:
    addr: "127.0.0.1:19090"

log:
  level: "debug"

data:
  database:
    dsn: "postgres://postgres:postgres@localhost:5432/project_test?sslmode=disable"
